import React from 'react';
import Stream from '../../sections/Stream/Stream';

const Streams = () => {
    return (
        <div>
            <Stream/>
        </div>
    );
}

export default Streams;
