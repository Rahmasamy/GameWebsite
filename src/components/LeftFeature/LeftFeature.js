import React from 'react';

const LeftFeature = (props) => {
    return (
        <><div className="left-feaures">
      {props.children}
          </div> 
        </>
    );
}

export default LeftFeature;
